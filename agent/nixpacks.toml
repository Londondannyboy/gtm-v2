# Railway/Nixpacks configuration for Python agent
providers = ["python"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python -m uvicorn src.agent:app --host 0.0.0.0 --port ${PORT:-8000}"
